// No migrations needed. Current schema is v1.
// Only add a migration here if you need to TRANSFORM existing persisted data
// (rename a field, convert a type, remove a field).
// Adding a new field with a default value does NOT require a migration —
// Zustand's persist middleware deep-merges stored state with initial state automatically.
